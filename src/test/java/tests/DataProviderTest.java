package tests;

import org.testng.Assert;
import org.testng.annotations.Test;
import tests.data.StaticProvide;

public class DataProviderTest {

    //@Test(dataProvider = "dataForSum", dataProviderClass = StaticProvide.class)
    //public void stepc(int a, int b, int expected)
    //{
     //   Assert.assertEquals(a + b,expected);
    //}
}
